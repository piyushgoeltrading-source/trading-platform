# PiyushTrade — Backend Dependencies
# Last updated: Phase 0
#
# CRITICAL: bcrypt MUST stay at 4.0.1
#           passlib is incompatible with bcrypt 4.1+
#           Do NOT run pip install --upgrade bcrypt

# ---------------------------------------------------------------------------
# Web framework
# ---------------------------------------------------------------------------
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22
starlette==1.0.0

# ---------------------------------------------------------------------------
# Database — PostgreSQL + SQLAlchemy 2.0 + Alembic
# ---------------------------------------------------------------------------
SQLAlchemy==2.0.48
psycopg2-binary==2.9.11
alembic==1.14.1

# ---------------------------------------------------------------------------
# Cache / Async jobs — Redis + Celery
# ---------------------------------------------------------------------------
redis==5.2.1
celery==5.4.0

# ---------------------------------------------------------------------------
# Auth — JWT + password hashing
# CRITICAL: bcrypt pinned to 4.0.1 — DO NOT UPGRADE
# ---------------------------------------------------------------------------
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# ---------------------------------------------------------------------------
# Config
# ---------------------------------------------------------------------------
pydantic==2.12.5
pydantic-settings==2.7.1
pydantic_core==2.41.5
python-dotenv==1.2.2

# ---------------------------------------------------------------------------
# AWS
# ---------------------------------------------------------------------------
boto3==1.37.1
botocore==1.37.1

# ---------------------------------------------------------------------------
# Observability (Phase 5 — safe to install now, only activates with SENTRY_DSN)
# ---------------------------------------------------------------------------
sentry-sdk==2.22.0

# ---------------------------------------------------------------------------
# Timezone support
# ---------------------------------------------------------------------------
pytz==2024.2

# ---------------------------------------------------------------------------
# Misc
# ---------------------------------------------------------------------------
annotated-types==0.7.0
anyio==4.13.0
cffi==2.0.0
click==8.3.1
colorama==0.4.6
cryptography==46.0.6
greenlet==3.3.2
h11==0.16.0
httptools==0.7.1
idna==3.11
pycparser==3.0
PyYAML==6.0.3
six==1.17.0
typing_extensions==4.15.0
watchfiles==1.1.1
websockets==16.0
